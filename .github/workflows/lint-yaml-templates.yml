name: Lint Yaml Codebase

on:
  push:
    branches:
      - 'feature/**'

  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - '**'

jobs:
  lint:
    uses: ./.github/workflows/lint-codebase-template.yml
    with:
      config_path: '.yamllint.yml'
