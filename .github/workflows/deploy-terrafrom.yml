name: Deploy Terraform

on:
  push:
    branches:
      - 'feature/**'

  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - '**'

jobs:
  plan:
    name: Terraform Plan
    uses: ./.github/workflows/template-plan-apply-terraform.yml
    secrets:
      AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }} 
    with:
      confitf_state_container_nameg_path: 'tfstate'
